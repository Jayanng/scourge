[workspace]
resolver = "2"
members = ["contracts/market", "contracts/factory", "contracts/mock-usdc"]

[profile.release]
opt-level = 3
debug = false
overflow-checks = true
lto = true
codegen-units = 1
panic = "abort"
incremental = false
